<h1>All Subjects</h1>
<div><%= link_to "Create New Subject", new_subject_path %></div>

<% @subjects.each do |subject| %>
	<li><%= subject.name %>
	<%= subject.fee %>
	<%= link_to "Delete",
				subject,
				:method => :delete %></li>

<% end %>
