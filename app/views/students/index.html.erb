<h1>All Students</h1>

<% @students.each do |student| %>
	<%= student.number %><%= link_to "edit", edit_student_path(student) %>
	<%= student.name %>
	<%= student.school %>
	<%= student.fee %>
	<%= student.subject %>

<% end %>
